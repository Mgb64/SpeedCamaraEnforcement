# SpeedCamaraEnforcement
Mobile-based automated speed enforcement system using YOLO. Detects vehicle plates and estimates speed in real time, flags those exceeding the limit, and logs violations (plate, speed, timestamp, and optional image). Designed as a lightweight traffic monitoring solution using only a smartphone camera.
